'use client';

// only name the file as 'loading' and can be shown during loading
const loading = () => {
  return <span className="text-xl capitalize">loading tour...</span>;
};

export default loading;
